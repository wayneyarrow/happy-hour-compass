# Copy this file to .env.local and fill in your real values.
# .env.local is gitignored — never commit real keys.

# ─── Supabase ─────────────────────────────────────────────────────────────────

# Project URL (Supabase Dashboard → Settings → API → Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Publishable key (Settings → API → API Keys → publishable — starts with sb_publishable_)
# Safe for browser and server use.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Secret key (Settings → API → API Keys — starts with sb_secret_)
# SERVER-SIDE ONLY — never prefix with NEXT_PUBLIC_ or expose to the browser.
SUPABASE_SECRET_KEY=sb_secret_...

# ─── Stripe — Billing & Payments ──────────────────────────────────────────────
# Use test keys (sk_test_ / pk_test_) for local dev and the Preview/staging
# environment. Use live keys (sk_live_ / pk_live_) only in the Production
# Vercel environment, once billing is ready to go live.
#
# Stripe Test mode and Live mode are entirely separate: separate Products/
# Prices, separate webhook endpoint registrations + signing secrets, and a
# separate Customer Portal configuration (Stripe Dashboard → Settings →
# Billing → Customer portal is configured independently per mode). Creating
# a Product/Price/webhook in Test mode does NOT create a Live-mode
# equivalent — each must be set up separately in the Stripe Dashboard's
# Test/Live toggle before Production can use it.
#
# Set these in Vercel per environment (Project Settings → Environment
# Variables → scope to Production vs Preview/Development) so Production
# resolves to Live values and Preview/Development resolve to Test values.

# Secret key (Stripe Dashboard → Developers → API Keys → Secret key)
# SERVER-SIDE ONLY.
STRIPE_SECRET_KEY=sk_test_...

# Publishable key (Stripe Dashboard → Developers → API Keys → Publishable key)
# Safe for browser use. Not currently read by any client-side code — Checkout
# and the Customer Portal are both server-created redirect sessions with no
# Stripe.js/Elements usage — but kept for parity in case that changes.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Price IDs for the monthly recurring plans
# (Stripe Dashboard → Product catalogue → [plan] → Pricing → copy the price ID)
# Must be created separately in Test mode and Live mode — a Test price ID
# will not resolve when STRIPE_SECRET_KEY is a live key, and vice versa.
# The dollar amounts here must match PLAN_PRICES in src/lib/planPresentation.ts
# ($9.99 Pro / $19.99 Premium) — that display copy is not read from Stripe.
STRIPE_PRO_PRICE_ID=price_...
STRIPE_PREMIUM_PRICE_ID=price_...

# Webhook signing secret — source depends on environment:
#
#   Local dev:   Run `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
#                The CLI prints a signing secret (whsec_...) — copy it here.
#                Restart the dev server after updating this value.
#
#   Vercel Preview (Test mode) / Production (Live mode):
#                Stripe Dashboard → Developers → Webhooks → [your endpoint]
#                → Signing secret → Reveal → copy the whsec_ value.
#                Test mode and Live mode each need their OWN endpoint
#                registered (same URL is fine — Test endpoints only fire for
#                Test-mode events) with their own signing secret.
#
# ⚠ The CLI secret and Dashboard secret are DIFFERENT values — do not mix them.
#   The CLI secret is temporary and changes each `stripe listen` session.
#   The Dashboard secret is permanent for the registered endpoint.
STRIPE_WEBHOOK_SECRET=whsec_...

# Canonical app URL — used to build Stripe Checkout success_url/cancel_url and
# Customer Portal return_url. Required in Vercel Production so Stripe redirects
# back to the right domain after checkout/portal.
# Without this, the app falls back to VERCEL_URL (deployment-specific), which
# changes on each deploy and can cause 404s on redirect.
# ⚠ Set this ONLY in the Production environment scope, not as a project-wide
#   value shared across environments — a single fixed value across
#   Production and Preview has previously leaked staging redirects into the
#   production deployment (see src/lib/email.ts's header comment for the
#   original incident with email links). Preview deployments ignore this
#   var by design (src/app/admin/subscription/stripeActions.ts's getAppUrl())
#   and always fall back to their own VERCEL_URL instead.
# Example: https://admin.happyhourcompass.com or https://happy-hour-compass.vercel.app
APP_URL=https://...

# ─── Website SEO / Canonical URL ──────────────────────────────────────────────

# Canonical public-facing website URL — used by metadata helpers, robots.txt,
# and sitemap generation (see src/lib/siteUrl.ts).
#
# This is SEPARATE from APP_URL:
#   APP_URL            → operator admin / Stripe redirect base URL
#   NEXT_PUBLIC_SITE_URL → public website canonical base URL (metadata, OG, sitemap)
#
# If omitted: Vercel production deployments auto-resolve to https://happyhourcompass.com;
# other environments fall back to VERCEL_URL (deployment-specific) or localhost:3000.
#
# Set this explicitly for staging and production Vercel environment settings:
#   Staging:    NEXT_PUBLIC_SITE_URL=https://staging.happyhourcompass.com
#   Production: NEXT_PUBLIC_SITE_URL=https://happyhourcompass.com
NEXT_PUBLIC_SITE_URL=https://...

# Search-engine indexing block flag.
# Set to "true" on staging and any environment that must never be indexed.
# NEVER set this to "true" on the production environment.
#
# When set: robots.txt returns "Disallow: /" and all page metadata includes
# <meta name="robots" content="noindex,nofollow">
#
# Staging Vercel project: NEXT_PUBLIC_NOINDEX=true
# Production Vercel project: leave unset (or explicitly set to "false")
# NEXT_PUBLIC_NOINDEX=true

# ─── Email (Resend) ───────────────────────────────────────────────────────────

# Resend API key (resend.com → API Keys)
# Used for transactional emails: claim approvals, operator invites, etc.
RESEND_API_KEY=re_...

# NOT READ BY THE APP — dead configuration, safe to remove wherever it's set.
# Internal ops notifications (new operator submissions, venue claims, operator
# account activations) are hardcoded to hello@happyhourcompass.com in
# getFounderNotificationEmail() (src/lib/email.ts). A hosted production
# incident found this env var overriding that address with a personal inbox
# left over from early testing, so it is intentionally no longer read.
# FOUNDER_NOTIFICATION_EMAIL=you@example.com

# ─── Brevo — Marketing/Lifecycle Communications (foundation only) ─────────────
# Phase 1 foundation only (src/lib/brevo/) — not yet wired into any live
# signup/account/confirmation flow. Resend (above) remains the only sender
# for transactional/system email; this is unrelated infrastructure.

# Brevo API key (Brevo dashboard → Settings → SMTP & API → API Keys).
# Server-only — never expose via NEXT_PUBLIC_. Production and the `website`
# Vercel Preview environment use separate Brevo accounts/keys.
BREVO_API_KEY=xkeysib-...

# Target Brevo list ID for consumer contact sync. Production: the "HHC
# Consumers" list. `website` Preview: the "HHC Staging Test" list. Always
# read via src/lib/brevo/config.ts — never hard-code a list ID at a call site.
BREVO_CONSUMER_LIST_ID=2

# Staging/Preview safety allowlist. When set, src/lib/brevo/stagingGuard.ts
# refuses to sync any email other than this one to Brevo, before any Brevo
# API call is made. Set ONLY on the `website` Vercel Preview environment —
# Production must not have this var set (its absence is what allows normal
# sync there; presence anywhere is what restricts it).
# BREVO_TEST_EMAIL=you@example.com

# Inbound webhook authentication token (Token auth) for Brevo's outbound
# Marketing webhook → POST /api/webhooks/brevo. Generate a random secret and
# enter the SAME value in Brevo's webhook "Token" field when the webhook is
# configured (after this endpoint is reviewed/deployed — see the Brevo
# integration foundation report). Server-only.
# BREVO_WEBHOOK_TOKEN=...

# Bearer token Vercel automatically sends to this project's cron-invoked
# routes when this env var is present (Vercel's own "Securing Cron Jobs"
# convention) — verified by src/app/api/cron/brevo-sync-outbox/route.ts.
# Also usable to trigger that route manually while no schedule exists yet
# (e.g. on `website` Preview, where Vercel Cron does not run at all).
# Generate a random secret; do not reuse another secret's value.
# CRON_SECRET=...

# ─── Google APIs ──────────────────────────────────────────────────────────────

# Google Maps JavaScript SDK — safe for browser use.
# Enable "Maps JavaScript API" for this key in Google Cloud Console.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIza...

# Google Places API (New) — SERVER-SIDE ONLY. Never prefix with NEXT_PUBLIC_.
# Used by the operator submission flow (/suggest/owner) to look up venues.
# Enable "Places API (New)" for this key in Google Cloud Console.
GOOGLE_PLACES_API_KEY=AIza...

# ─── Google Analytics 4 (Public Website only) ─────────────────────────────────
# Measurement ID from GA4 Admin → Data Streams → [web stream] → Measurement ID.
# Additive only — does not affect the first-party analytics pipeline
# (src/lib/analytics.ts, @vercel/analytics, /api/track/*) that remains the
# source of truth for Operator Analytics, Founder Control Panel, Venue Health,
# and Action Center. Wired up only in app/(website)/layout.tsx, so it never
# fires on Operator Admin or Founder Control Panel routes.
# Leave unset to keep an environment (e.g. local dev) untracked.
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-...

# ─── Slack (Ops Alerts) ───────────────────────────────────────────────────────
# Used to send internal ops notifications (payment failures, critical errors).
# Create incoming webhooks at api.slack.com → your app → Incoming Webhooks.
# Optional: alerts are silently skipped if these are not set.

SLACK_OPS_CRITICAL_WEBHOOK_URL=https://hooks.slack.com/services/...
SLACK_OPS_ALERTS_WEBHOOK_URL=https://hooks.slack.com/services/...

# ─── Slack (Acquisition Channels) ─────────────────────────────────────────────
# Used for proactive notifications on new venue suggestions, submissions, claims,
# contact messages, cancellations, and consumer signups. Each maps to a
# dedicated Slack channel. Optional: notifications are silently skipped if
# these are not set.

SLACK_VENUE_SUGGESTIONS_WEBHOOK_URL=https://hooks.slack.com/services/...
SLACK_VENUE_SUBMISSIONS_WEBHOOK_URL=https://hooks.slack.com/services/...
SLACK_VENUE_CLAIMS_WEBHOOK_URL=https://hooks.slack.com/services/...
SLACK_WEBSITE_CONTACT_WEBHOOK_URL=https://hooks.slack.com/services/...
SLACK_VENUE_CHURN_WEBHOOK_URL=https://hooks.slack.com/services/...
SLACK_CONSUMER_SIGNUP_WEBHOOK_URL=https://hooks.slack.com/services/...
SLACK_VENUE_PLAN_CHANGES_WEBHOOK_URL=https://hooks.slack.com/services/...

# ─── Admin & Control Panel ────────────────────────────────────────────────────

# Comma-separated list of email addresses allowed to access /control-panel/*.
# These are internal platform admin accounts — not operator accounts.
# Example: admin@example.com,ops@example.com
CONTROL_PANEL_ADMIN_EMAILS=

# ─── Sentry — Application Error Monitoring ────────────────────────────────────
# https://sentry.io → happy-hour-compass org → javascript-nextjs project

# DSN (Data Source Name) — safe to expose publicly; write-only for event ingestion.
# Found at: Sentry → Settings → Projects → javascript-nextjs → Client Keys (DSN)
# Must be prefixed NEXT_PUBLIC_ because it is used by the client-side SDK.
NEXT_PUBLIC_SENTRY_DSN=https://xxxxxxxxxxxx@oxxxxxx.ingest.sentry.io/xxxxxxx

# Environment tag attached to every Sentry event.
# Omit for local dev — sentry.client.config.ts defaults to NODE_ENV.
# Set to "production" in Vercel production environment settings.
# NEXT_PUBLIC_SENTRY_ENVIRONMENT=production

# ── Source map upload (optional on Vercel Hobby) ──────────────────────────────
# Without these, Sentry still captures errors — stack traces will show
# minified code. Add them when you're ready to see human-readable traces.
#
# Auth token: Sentry → Settings → Auth Tokens → Create new token
#   (needs project:releases and org:read scopes)
# SENTRY_AUTH_TOKEN=sntrys_...
#
# Organization slug: Sentry → Settings → General → Organization Slug
# SENTRY_ORG=happy-hour-compass
#
# Project slug: Sentry → Settings → Projects → javascript-nextjs → General → Name
# SENTRY_PROJECT=javascript-nextjs
